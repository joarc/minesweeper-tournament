{
  "rules": {
    ".read": true,
    "tournament": {
      "$tournament": {
        "$score": {
          ".write": "!data.exists() && newData.child('name').val() != null && newData.child('score').val() != null && newData.child('timestamp').val() != null && (root.child('activeTournament').val() != null || root.child('activeTournament').val() != '')"
        }
      }
    },
    "global": {
      ".write": "!data.exists() && newData.child('name').val() != null && newData.child('score').val() != null && newData.child('timestamp').val() != null",
      "$score": {
      	".write": "!data.exists() && newData.child('name').val() != null && newData.child('score').val() != null && newData.child('timestamp').val() != null"
      }
    }
  }
}
